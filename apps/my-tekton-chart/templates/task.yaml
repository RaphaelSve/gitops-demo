apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: {{ .Values.taskName }}
spec:
  steps:
    - name: echo
      image: alpine
      script: |
        #!/bin/sh
        echo "Hello from Tekton Task"

